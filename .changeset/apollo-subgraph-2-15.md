---
"@commercetools/cli-topic-integration-layer": patch
---

`extension build --all` and `extension serve` build subgraphs with the module list `@apollo/subgraph` 2.15 requires. The plugin now depends on the current Apollo composition, gateway, subgraph, and envelop releases.
