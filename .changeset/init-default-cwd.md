---
"@commercetools/cli-topic-integration-layer": minor
---

`integration-layer init` now defaults to the current directory when the target is empty, so you can scaffold an extension in place without passing a path.
